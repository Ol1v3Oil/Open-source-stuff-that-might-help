import random
def rolls():
  #odds of a choice
  dice = [100,200,200,200]
  rolls = []
    
  for i in range(1,4):
    rolls.append(float(random.randrange(dice[0],dice[i])/100))
  
  #print the odds
  print(rolls)
  #gets the highest of the odds
  print(rolls.index(max(rolls)))
